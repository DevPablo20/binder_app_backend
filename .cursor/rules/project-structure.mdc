---
description: Directory structure, key files, and module layout for binder_app_backend
alwaysApply: true
---

# Project Structure

NestJS 11 + TypeORM + PostgreSQL API for BI binder app — the semantic brain of a marketing data lake. See [domain-architecture.mdc](mdc:.cursor/rules/domain-architecture.mdc) for layered domain design (Access, Business, Media, Bridge).

## Root Layout

```
binder_app_backend/
├── src/                    # Application source (edit here)
├── dist/                   # Compiled output — do not edit
├── infra/postgres_data/    # Local Postgres volume (Docker, gitignored)
├── .cursor/rules/          # Cursor agent rules
├── compose.yaml            # Dev stack: app + postgres_local
├── Dockerfile
├── .env.development        # Dev env vars
├── nest-cli.json           # sourceRoot: src
├── package.json
├── tsconfig.json
└── tsconfig.build.json
```

## Entry Points

| File | Role |
|------|------|
| `src/main.ts` | Bootstrap: Swagger (`/api`), CORS, cookie-parser, global ValidationPipe |
| `src/app.module.ts` | Root module — imports all feature modules |

## Source Tree (`src/`)

```
src/
├── main.ts
├── app.module.ts
├── auth/
│   ├── auth.module.ts          # JWT, global AuthGuard (APP_GUARD), imports MailModule
│   ├── auth.controller.ts      # login, logout, password/forgot, password/reset
│   ├── auth.service.ts         # login + password reset; injects MailService
│   ├── auth.service.spec.ts    # forgot/reset behavior contract
│   ├── auth.guard.ts
│   ├── auth.dto.ts             # LoginDTO, ForgottenPasswordDTO, ResetPasswordDTO
│   ├── userSignature.type.ts
│   └── decorators/
│       ├── public.decorator.ts   # @Public() — skip auth
│       ├── private.decorator.ts  # @Private(Role.X) — role check
│       └── currentUser.decorator.ts
├── user/
│   ├── user.module.ts
│   ├── user.controller.ts
│   ├── user.service.ts
│   └── user.entity.ts
├── user-company/
│   ├── user-company.module.ts    # service-only; exports UserCompanyService
│   ├── user-company.controller.ts # PATCH revoke, PUT sync (Superadmin)
│   ├── user-company.service.ts   # grant/revoke/sync memberships (Superadmin)
│   ├── user-company.dto.ts
│   └── user-company.entity.ts
├── company/
│   ├── company.module.ts
│   ├── company.controller.ts
│   ├── company.service.ts
│   └── company.entity.ts
├── client/
│   ├── client.module.ts          # entities only; no routes yet
│   └── client.entity.ts          # Business layer; FK to Company
├── campaign/
│   ├── campaign.module.ts        # entities only; no routes yet
│   └── campaign.entity.ts        # Business layer; FK to Client
├── platform/
│   ├── platform.module.ts        # entities only; no routes yet
│   ├── platform.entity.ts        # Media layer (global)
│   ├── channel.entity.ts         # Media layer; FK to Platform
│   └── buying-type.entity.ts     # Media layer (global); M:N via channel_buying_type
├── format/
│   ├── format.module.ts          # entities only; no routes yet
│   ├── format.entity.ts          # Media layer (global)
│   └── sub-format.entity.ts      # Media layer; FK to Format
├── grouping/
│   ├── grouping.module.ts        # entities only; no routes yet
│   ├── grouping.entity.ts        # Media layer; FK to Campaign
│   └── sub-grouping.entity.ts    # Media layer; FK to Grouping
├── bridge/
│   ├── bridge.module.ts            # entities only; no routes yet
│   ├── platform-account.entity.ts  # Bridge; FK to Client + Platform
│   └── platform-object-map.entity.ts # Bridge; ETL object → Campaign + media FKs
├── invite/
│   ├── invite.module.ts
│   ├── invite.controller.ts
│   ├── invite.service.ts
│   └── invite.entity.ts
├── mail/
│   ├── mail.module.ts          # exports MailService only (no controller)
│   └── mail.service.ts         # MailerSend: sendMail, sendInviteEmail, sendPasswordResetEmail
├── database/
│   ├── database.module.ts      # TypeOrmModule.forRootAsync
│   ├── typeorm.config.ts       # Shared config (NestJS + CLI)
│   ├── typeormFile.ts          # TypeORM CLI data source
│   ├── migrations/
│   └── seeds/default.ts
└── common/
    ├── role.enum.ts            # Superadmin, Editor, Viewer
    └── platform-object-type.enum.ts  # campaign, ad_set, ad (Bridge ETL join)
```

## Active Modules

| Module | Registered in `AppModule` | TypeORM |
|--------|---------------------------|---------|
| `DatabaseModule` | Yes (first) | `forRootAsync` via `buildTypeOrmConfig()` |
| `AuthModule` | Yes | `forFeature([User, UserCompany])`; imports `MailModule` |
| `UserModule` | Yes | `forFeature([User, UserCompany])`; imports `UserCompanyModule` |
| `UserCompanyModule` | No (imported by `UserModule`) | `forFeature([UserCompany, User, Company])`; exports `UserCompanyService` only |
| `CompanyModule` | Yes | `forFeature([Company, UserCompany])` |
| `InviteModule` | Yes | `forFeature([Invite, User, Company])` |
| `ClientModule` | Yes | `forFeature([Client, Company])` — entities only, no routes |
| `CampaignModule` | Yes | `forFeature([Campaign, Client])` — entities only, no routes |
| `PlatformModule` | Yes | `forFeature([Platform, Channel, BuyingType])` — `channel_buying_type` junction |
| `FormatModule` | Yes | `forFeature([Format, SubFormat])` — entities only, no routes |
| `GroupingModule` | Yes | `forFeature([Grouping, SubGrouping, Campaign])` — entities only, no routes |
| `BridgeModule` | Yes | `forFeature([PlatformAccount, PlatformObjectMap, ...])` — `platform_object_map_sub_grouping` junction |
| `MailModule` | Yes | — (service-only; consumed by `AuthModule`) |

## TypeORM Pattern

- **Root connection**: `DatabaseModule` → `TypeOrmModule.forRootAsync` + `ConfigService`
- **Feature repos**: `TypeOrmModule.forFeature([Entity])` in each module that needs DB access
- **Injection**: `@InjectRepository(Entity)` — never use custom provider tokens
- **CLI**: `typeormFile.ts` reuses `buildTypeOrmConfig()` from `typeorm.config.ts`
- **Entities glob**: `src/**/*.entity.ts` (compiled to `dist/**/*.entity.js`)

## Auth

- Global `AuthGuard` registered via `APP_GUARD` in `AuthModule`
- JWT stored in `access_token` cookie (also accepts `Authorization` header)
- `@CurrentUser()` returns `UserSignature` (`id`, `name`, `role`, `companyIds`)
- `Role` enum lives in `src/common/role.enum.ts` — not in `auth/`
- Public routes: `POST /auth/login`, `POST /auth/logout`, `POST /auth/password/forgot`, `POST /auth/password/reset`

## Cross-Module Wiring

- `AuthModule` imports `MailModule` to send password-reset emails from `AuthService`
- `MailModule` exports `MailService` only — no HTTP routes; email is triggered by auth, not by direct mail endpoints
- Feature modules that need email should import `MailModule` and inject `MailService` — do not instantiate MailerSend outside `mail.service.ts`
- `UserModule` imports `UserCompanyModule` for membership read projections and `UserCompanyService`

## Conventions

- **New feature**: create `src/<feature>/` with module, controller, service, entity; add `TypeOrmModule.forFeature([Entity])`; import in `app.module.ts`
- **Shared enums/types**: place in `src/common/`
- **Module order in `AppModule`**: `DatabaseModule` must load before modules that use TypeORM
- **Env config**: global `ConfigModule` reads `./.env.${process.env.NODE_ENV}`
- **Do not edit**: `dist/`, `node_modules/`, `infra/postgres_data/`

## Commands

```bash
npm run start:dev                                              # Dev server
docker compose up                                              # App :8090, Postgres :55432
npm run build && npm run migration:generate src/database/migrations/<name>
npm run migration:run                                          # Apply migrations
npm run migration:show                                         # Migration status
npm run seed:run                                               # Run default seed
```
